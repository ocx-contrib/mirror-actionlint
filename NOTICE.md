# NOTICE

This repository packages and redistributes upstream [actionlint](https://github.com/rhysd/actionlint).

The Apache-2.0 license covers the OCX pipeline files authored here. It does
**not** cover upstream-derived assets — the actionlint binaries published to
`ocx.sh/actionlint` (MIT, © rhysd) and the actionlint name/logo (used for
catalog identification under nominative-fair-use).
